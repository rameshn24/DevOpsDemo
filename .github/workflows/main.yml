steps:
  - name: Checkout code
    uses: actions/checkout@v4
  - name: Copy file to destination
    run: |
      mkdir -p destination/TO
      cp Source/test.txt destination/TO/
      echo "Copied Source/test.txt to destination/TO/"
  - name: Commit and push changes
    run: |
      git config user.name "ramesh24"
      git config user.email "n.ramesh.n@gmail.com"
      git add .
      git commit -m "Automated file copy"
      git push
